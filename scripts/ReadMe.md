The bmcharts.py is a python2 script that takes the results directory from benchmaster.sh script as it's only argument.
output are png files of the results in /root/*.png
requires pip2 install matplotlib
